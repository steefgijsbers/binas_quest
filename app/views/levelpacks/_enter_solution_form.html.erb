<% unless @level.nil? %>
<%= form_tag(check_solution_path, method: 'post') do %>
	<%= label_tag(:guess, "Antwoord: ") %>
	<%= text_field_tag(:guess) %>

	<%= submit_tag("Check") %>
<% end %>
<% end %>
