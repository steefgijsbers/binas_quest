<%= form_tag(check_solution_path, method: 'post') do %>
	<%= label_tag(:guess, "Antwoord: ") %>
	<%= text_field_tag(:guess) %>
	<%= hidden_field_tag(:levelpack_id, @levelpack.id) %>
	<%= hidden_field_tag(:level_id, @level.id) %>
	<%= submit_tag("Check") %>
<% end %>
